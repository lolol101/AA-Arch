#include "algorithm.hpp"

std::vector<std::vector<size_t>> algo::findAllStringsAhoCorasick(const std::string &text, const std::vector<std::string> &patterns)
{
    return std::vector<std::vector<size_t>>(patterns.size());
}
